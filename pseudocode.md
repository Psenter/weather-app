# PROGRAM START
CONNECT API to page

```
function RENDER() {

    //Show text bar for zip code

    //Show button for search

    //take user input and run through api

    if (zip code is not 5 numbers){
        return "zip code is incorrect"
    }
    else {
        run function normally
    }

    FUNCTION zipCode(userInput) {

    //run zip entered by user through API

    //return API data

    //show API data in HTML for user to view
    }

    FUNCTION newZipButton () {

        //resets the page back to beginning

        //lets put in new zip code
    }
}

RENDER();
```
# END